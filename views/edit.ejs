<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

    <p>Hi</p>
    <div><%= todo.todo %></div>
    <!-- needs to be a form where you display
    the current element for todo.todo, todo.tags etc,
but can change them  -->
     <form action="/todos/edit/<%=todo._id%>" method="POST"> 
        <input type="hidden" name="_method" value="PUT">
        <div class="addTodo">
            <div class="addTodo">
                <label>Todo:</label>
                <input type="text" name="todo" placeholder="todo" value="<%= todo.todo %>">
                <label>Description (optional):</label>
                <textarea  name="todoDetails" placeholder="description" value="<%= todo.todoDetails ? todo.todoDetails : '' %>"></textarea>
                 
                <div>
                <% if(todo.tags.length >= 1){ %>
                    <% todo.tags.forEach(function(tag){ %>
                        <input type="text" name="tag" value='<%= tag %>'>
                <% }); %>
                <% } else{ %>  
                    <% for(let i=1;i<=5;i++){ %>
                        <% if(todo.tags[i-1]) {%>
                            <input type="text" name="tag<%=i%>" placeholder='tag <%= i %>'> 
                        <% } %>
     
                   <% } %>
                <% } %> 
            </div>
                <input type="submit">
            </div>
        </div>
    </form>
    
    <script src="js/main.js"></script>
</body>
</html>